{
  "mcpServers": {
    "engram-lite": {
      "command": "engram-lite-mcp",
      "env": {
        "ENGRAM_USER_DB": "${HOME}/.engram/engram.db",
        "ENGRAM_PROJECT_DB": "${PWD}/.engram/engram.db"
      }
    }
  }
}
